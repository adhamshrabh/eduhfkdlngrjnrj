# تشغيل منصّة الروضة — دليل عضو الفريق

هذه النسخة تحوي **الشفرة المصدرية كاملةً** (٣٦٠ ملفاً) وتاريخ Git، ولا تحوي
ما يمكنك توليده أو تنزيله بنفسك. اتبع الخطوات أدناه ويصير المشروع عاملاً
عندك خلال دقائق.

---

## ١. المتطلّبات

| الأداة | النسخة | التحقّق |
|---|---|---|
| Node.js | ≥ ٢٢ | `node --version` |
| Python | **٣٫١٢ بالضبط** | `python --version` |
| Git | أي حديثة | `git --version` |

> ⚠️ **٣٫١٢ ليست «فأعلى»، وهذا مُختبَر لا مفترَض.** `requirements.txt` يثبّت
> `Pillow==10.4.0`، ولا توجد له حزمة جاهزة لبايثون ٣٫١٤ — فيحاول pip بناءه
> من المصدر ويفشل بلا مترجم C. جُرِّب على ٣٫١٤ ففشل، وبيئة المشروع العاملة
> على ٣٫١٢٫٢. إن كان لديك ٣٫١٤، ثبّت ٣٫١٢ بجوارها وأنشئ البيئة بها:
>
> ```bash
> py -3.12 -m venv .venv        # ويندوز
> python3.12 -m venv .venv      # لينكس/ماك
> ```

Docker اختياري — للتشغيل بأمر واحد بدل الخطوات اليدوية (§٦).

---

## ٢. ما **لا** تجده في هذه النسخة، ولماذا

كلّها مُستبعَدة لأنها تُولَّد أو تُنزَّل، لا لأنها ناقصة.

| المُستبعَد | الحجم تقريباً | كيف تستعيده |
|---|---|---|
| `frontend/node_modules/` | مئات الميغابايت | `npm install` (§٣) |
| `backend/.venv/` | ~١٥٠ ميغابايت | `pip install -r requirements.txt` (§٤) |
| `backend/media/` | ~١٢٠ ميغابايت | صور القصص وأصواتها — **انظر §٥** |
| `backend/db.sqlite3` | متغيّر | `manage.py migrate` ينشئها فارغة (§٤) |
| `backend/.env` | سطور | انسخ `.env.example` — **لا يُشارَك أبداً، فيه أسرار** |
| `frontend/apps/*/dist/` | عشرات الميغابايت | `npm run build` عند الحاجة |
| `__pycache__/` وملفات الاختبار المؤقّتة | — | تُولَّد وحدها |

> **قاعدة:** لو استُبعد شيء لا تستطيع توليده بأمر، فذلك خطأ — أبلغ صاحب
> المشروع بدل أن تخمّن.

---

## ٣. الواجهة الأمامية

```bash
cd frontend
npm install
```

> **إن فشل التنزيل** (`ECONNRESET` أو انتهاء مهلة): المشكلة في الشبكة لا في
> المشروع — وقد واجهناها أثناء التحقّق من هذه النسخة نفسها. جرّب مرآةً
> قريبة، أو اطلب من صاحب المشروع أرشيف `node_modules` جاهزاً.

أمرٌ واحد يستعيد `node_modules` لمساحات العمل الثلاث (`@edu/engine`،
`@edu/web`، `@edu/studio`) — فهي مساحات عمل npm، ولا تُثبَّت كلٌّ على حدة.

**تحقّق أن كل شيء سليم:**

```bash
npm run typecheck     # الأنواع في المساحات الثلاث
npm test              # ~١٬٥٠٠ اختبار
```

---

## ٤. الخادم

```bash
cd backend
python -m venv .venv

# ويندوز
.venv\Scripts\activate
# لينكس/ماك
source .venv/bin/activate

pip install -r requirements.txt
cp .env.example .env          # ثم افتحه واملأ القيم
python manage.py migrate
python manage.py seed_demo    # حسابات تجريبية
```

**حسابا التجربة بعد `seed_demo`:**

| الدور | البريد | كلمة المرور |
|---|---|---|
| مديرة | `admin@rawda.local` | `admin12345` |
| معلّمة | `muallima@rawda.local` | `teacher12345` |

⚠️ هذان للتطوير المحلّي فقط. لا يُنشران على أي خادم.

---

## ٥. المحتوى (الصور والأصوات)

**غير موجود في هذه النسخة** — وهو ~١٢٠ ميغابايت، أي أضعاف الشفرة كلّها.

بعد `migrate` تكون قاعدة البيانات فارغة: لا قصص ولا صور. وهذا **كافٍ تماماً**
للعمل على المحرّك أو الاستوديو أو الخادم — تُنشئ قصّة جديدة من الاستوديو
وترفع صورتين وتعمل.

**وإن احتجت المحتوى الحقيقي** (للعمل على العرض أو على قصّة بعينها)، اطلب من
صاحب المشروع حزمةً تحوي:

```
backend/media/stories/<slug>/     ملفات القصّة
نسخة قاعدة البيانات               pg_dump أو db.sqlite3
```

تُفكّ في المكانين نفسهما ويعمل كل شيء.

> **ملاحظة مفيدة:** المحرّك مصمَّم ليتنازل عن الأصل المفقود لا عن القصّة —
> فقصّة بلا صور تُعرض بمربّعات فارغة وتبقى قابلة للّعب. هذا سلوك مقصود
> وموثّق، لا عطل.

---

## ٦. التشغيل

ثلاث عمليات في ثلاث طرفيّات:

```bash
# ١ — الخادم
cd backend && python manage.py runserver 8000

# ٢ — المنصّة
cd frontend && npm run dev          # http://localhost:5173

# ٣ — الاستوديو
cd frontend && npm run dev:studio   # http://localhost:5174
```

**ادخل من المنصّة (5173) أولاً**، ثم افتح الاستوديو من زرّه داخل التطبيق —
عندها يصل رمز الدخول ويعمل الحفظ. فتحُ `5174` مباشرةً يعطي قراءةً فقط.

**أو بأمر واحد عبر Docker:**

```bash
DJANGO_SECRET_KEY=<أي-نص-طويل> docker compose up --build
```

---

## ٧. الاختبارات

```bash
cd frontend && npm test              # engine · studio · web
cd backend && python manage.py test  # Django
```

المشروع اليوم عند **~١٬٥٨٠ اختباراً ناجحاً**. إن فشل شيء قبل أن تلمس
الشفرة، فالمشكلة في الإعداد لا في الشفرة — راجع §٣ و§٤.

---

## ٨. اقرأ هذه قبل أن تكتب سطراً

مجلّد `docs/` ليس توثيقاً مكمّلاً بل **العقد نفسه**:

| الملف | لماذا يهمّك |
|---|---|
| `Scene-Model-Specification-v1.0.md` | عقد نموذج المشهد — مجمّد، وكل تغيير يأتي كرقعة مرقّمة |
| `Scene-Model-Specification-v1.0.20 … 24.md` | أحدث الرقع: الجواب بالبطاقة، الترتيب، الإطار والأزرار |
| `Security-Requirements-v1.md` | ٥١ متطلّباً أمنياً قبل النشر العامّ |

**ثلاث قواعد تحكم هذا المشروع:**

1. **المحرّك يُبقي قصّة الطفل قابلة للّعب.** خطأ التأليف يتنازل ولا يتجمّد،
   والخطأ يُقال في الاستوديو حيث يمكن إصلاحه.
2. **الترتيب: العقد ← المُتحقِّق ← المحرّك ← الاستوديو.** لا تبدأ من الواجهة.
3. **التعليقات تشرح «لماذا» لا «ماذا»**، وتوثّق الأعطال التي وقعت فعلاً.
   اقرأ رؤوس الملفات قبل تعديلها — فيها دروسٌ مدفوعة الثمن.

---

## ٩. حين تتعثّر

| العرض | الأرجح |
|---|---|
| صفحة بيضاء في الاستوديو | لم تدخل من المنصّة أولاً (§٦) |
| «القصة غير موجودة» | قاعدة بيانات فارغة — طبيعي، أنشئ قصّة |
| صور فارغة في العرض | لا محتوى في هذه النسخة (§٥) |
| فشل `npm install` | نسخة Node أقدم من ٢٢، أو الشبكة (§٣) |
| فشل `pip install` عند Pillow | **بايثون غير ٣٫١٢** — انظر §١ |
| فشل اختبارات الخادم | لم تُفعّل البيئة الافتراضية |
