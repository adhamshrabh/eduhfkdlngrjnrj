#pragma once

// ── أرجل الصندوق ──────────────────────────────────────────────────────────
//
// القارئ على SPI: SCK=18، MISO=19، MOSI=23 (الافتراضي، لا يُعرَّف هنا).
#define RST_PIN 22
#define SS_PIN   5

// ⚠️ D34 و D35 مدخلان **فقط**، وبلا مقاومة رفع داخلية.
//
// هذا ليس تفصيلاً: `INPUT_PULLUP` عليهما لا يفعل شيئاً، فيطفو الطرف ويقرأ
// ضغطات وهمية لا يفسّرها شيء — زرّ «يضغط نفسه» أمام الصف. لكلٍّ منهما
// مقاومة رفع خارجية 10kΩ إلى 3.3V، والزرّ يوصّل إلى الأرضي.
//
// الترتيب هنا **هو** ترتيب الخيارات على الشاشة: الأول يختار الأول.
static const uint8_t BUTTON_PINS[] = {35, 25, 33, 32, 34};
static const bool    BUTTON_NEEDS_EXTERNAL_PULLUP[] = {true, false, false, false, true};

// مصباح لكل زرّ، بالترتيب نفسه.
//
// ⚠️ D12 رجل تقييد (MTDI): إن كانت مرفوعة إلى 3.3V لحظة الإقلاع ضبطت جهد
// الفلاش خطأً فلا تُقلع اللوحة. مصباح مع مقاومة إلى الأرضي آمن؛ أي رفع
// عليها ليس كذلك.
static const uint8_t LED_PINS[] = {13, 12, 14, 27, 26};

static const uint8_t BUTTON_COUNT = sizeof(BUTTON_PINS);
