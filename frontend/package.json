{
  "name": "edu-platform-frontend",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@edu/web",
    "dev:studio": "npm run dev --workspace=@edu/studio",
    "build": "npm run build --workspace=@edu/web && npm run build --workspace=@edu/studio",
    "test": "npm run test --workspace=@edu/engine && npm run test --workspace=@edu/studio",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^4.1.10",
    "jsdom": "^29.1.1"
  }
}
