/**
 * shared/index.ts
 *
 * Barrel for the entire shared layer. Pulling from `@shared` re-exports both
 * the type catalog and the runtime utilities in one place.
 */

export * from "./types";
export * from "./utils";
